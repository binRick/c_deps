greatest_lib = library('greatest', 
  install: false, dependencies: [], 
  include_directories: [
    inc,
  ]
)
greatest_dep = declare_dependency(include_directories: [inc], link_with: greatest_lib)
