subprocess_lib = library('subprocess',
  install: false, 
  dependencies: [
  ], 
  include_directories: [
  '../../../submodules/subprocess.h',
    inc,
  ]
)
subprocess_dep = declare_dependency(include_directories: [inc], link_with: subprocess_lib)
