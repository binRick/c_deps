memory_leak_detector_srcs = [files(

)]

memory_leak_detector_dirs = [
  '../../../submodules/memory_leak_detector',
  inc,
]

memory_leak_detector_deps = [

]


memory_leak_detector_dep = declare_dependency(
  include_directories: memory_leak_detector_dirs,
  link_with: [],
)
