libspinner_lib = library('libspinner',
  install: false, 
  dependencies: [
  ], 
  include_directories: [
    inc,
  ]
)
libspinner_dep = declare_dependency(include_directories: [inc], link_with: libspinner_lib)
